_9b0‍.x([["cli",()=>cli]]);const arg = require('arg');

const args = arg({
    
  '--help': Boolean,
  '--version': Boolean,
  '--lunch': Boolean,
  '--dinner': Boolean,

   //Aliases
   '-h': '--help',
   '-v': '--version',
   '-l': '--lunch',
   '-d': '--dinner'
})

       function cli(){
if(args["--help"]) 
console.log('You typed help')
}
